use crate::utils::mymath;

pub mod mat {
    pub struct Mat {
        pub greet: String,
        // shape: Vec<i32>,
    }

    // impl Mat {
    //     pub fn get_shape(&self) ->Vec<i32> {
            
    //     }
    // }
}